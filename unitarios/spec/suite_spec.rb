
describe "Suite de testes rspec" do
  it "soma de dois valores sucesso" do
    soma = 10 + 5
    expect(soma).to eql(15)
  end

  it "soma de dois valores sucesso" do
    soma = 10 + 5
    expect(soma).to eql(15)
  end

  it "soma de dois valores sucesso" do
    soma = 10 + 5
    expect(soma).to eql(15)
  end

  it "soma de dois valores erro" do
    soma = 10 + 2
    expect(soma).to eql(12)
  end
end
